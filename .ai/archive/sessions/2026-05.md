# 2026-05 Archive

## 2026-05-18 - Legacy AI Architecture Compatibility Migration

### Scope

- Inspect `F:\newAPI` as a read-only legacy reference.
- Determine whether the current Fuxi API project fully migrated the old AI architecture and documentation rules.
- Migrate missing reusable governance pieces without importing old stack assumptions.
- Define the release-closure meaning that was active before the later production-release clarification.

### Findings

- Old `F:\newAPI` had a richer AI collaboration model:
  - Root `AGENTS.md` and `CLAUDE.md`
  - `.ai/README.md`, `.ai/router.md`, `.ai/shared_instructions.md`, `.ai/rules.md`, `.ai/code_style.md`, `.ai/project_guide.md`
  - `.ai/systems/*.md`
  - `.ai/generated/*.md`
  - `.ai/MEMORY.md`, `.ai/sessions.md`, `.ai/archive/sessions/`
  - `.agents/skills/update-server`, `update-staging-server`, and `local-preview`
  - `tools/check_ai_docs.py`
- Current project already had the high-level `.ai` skeleton and adapted release skills.
- Current project was not fully compatible with the old governance model because it lacked:
  - AI docs health checker
  - Router `last_verified` schema
  - Assistant execution guide
  - Runtime and AI-doc generated snapshots
  - Explicit release-closure semantics that were later superseded by the production-release clarification

### Migration

- Added `tools/check_ai_docs.py`.
- Added `tools/MANIFEST.md`.
- Added `.ai/assistant_guide.md`.
- Updated `.ai/router.md` to the legacy-compatible six-column schema:
  - `scope/path_glob`
  - `status`
  - `must_read`
  - `related`
  - `keywords`
  - `last_verified`
- Added `.ai/generated/runtime-summary.md`.
- Added `.ai/generated/ai-doc-health-summary.md`.
- Updated `.ai/generated/skills-index.md`.
- Updated `.ai/README.md`, `.ai/shared_instructions.md`, `.ai/systems/ai-doc-governance.md`, and `.ai/systems/release-and-environments.md`.
- Updated root `AGENTS.md` and `CLAUDE.md` to expose the migrated AI workflow and release-closure semantics at the project entry point.

### Release Semantics Captured

`更新发布版` and `更新发布版并归档` now mean full production release, not a simple version edit:

1. Inspect current worktree and target changes.
2. Bump patch version by default unless minor/major is explicitly requested or the requested release already exists.
3. Run relevant local verification.
4. Commit and push release changes to Git.
5. Publish or update the GitHub tag/Release.
6. Deploy production so `https://fuxiapi.top/` is actually updated.
7. Verify production and archive scope, verification, release status, and risks in `.ai/MEMORY.md`, `.ai/sessions.md`, and `.ai/archive/sessions/`.

### Legacy Boundary

- Old new-api docs remain reference-only.
- Do not import these old active assumptions into current work:
  - GORM model layer
  - React/Rsbuild/Bun frontend
  - old `/data/new-api/**` release scripts as active deploy commands
  - old `new-api` / `new-api-staging` containers as the primary runtime

### Verification

- `go test ./...`: passed
- `npx pnpm --dir frontend run typecheck`: passed
- `npx pnpm --dir frontend run lint:check`: passed
- `python tools/check_ai_docs.py --summary --details`: passed, 0 failures, 0 warnings.
- Context recall samples passed:
  - `backend/cmd/server/VERSION`
  - `frontend/src/views/admin/AccountsView.vue`
  - `deploy/fuxi/deploy-staging.sh`
- `python -m py_compile tools/check_ai_docs.py`: passed
- `git diff --check`: passed with Windows LF/CRLF warnings only.

### Release Prep

- Commit: `41d81ff1 Prepare 0.1.127 account table release`
- Pushed to: `origin/main`
- Version: `0.1.127`
- Tag: `v0.1.127`
- GitHub Release: `https://github.com/jiutubaba/fx-api/releases/tag/v0.1.127`
- Release workflow: `26035687293`, success
- Tag CI workflow: `26035687297`, success
- Tag Security Scan workflow: `26035687263`, success
- Deployment status: no production deploy during this earlier release-prep session; `v0.1.127` was deployed in the later production release entry below.

## 2026-05-18 - Account Table Column Resize Release Prep

### Scope

- Make the admin account-management table easier to inspect after importing the full account pool.
- Support left/right drag resizing from the table header.
- Prepare the repository release version for the next build.

### Code Changes

- `frontend/src/components/common/DataTable.vue`
  - Added optional column width support through `colgroup` and fixed table layout.
  - Added pointer-based resize handles on resizable headers.
  - Emits `columnResize` and prevents accidental sort clicks during resize.
  - Applies column widths to loading rows and body cells.
  - Rechecks horizontal scroll state after width changes.
- `frontend/src/components/common/types.ts`
  - Added `width`, `minWidth`, `maxWidth`, and `resizable` to the shared `Column` type.
- `frontend/src/views/admin/AccountsView.vue`
  - Added default/min/max widths for account columns.
  - Persists custom widths to `localStorage` key `account-column-widths`.
  - Keeps the select column fixed and allows other account columns to resize.
- `frontend/src/i18n/locales/zh.ts` and `frontend/src/i18n/locales/en.ts`
  - Added accessible resize-label translations.
- `backend/cmd/server/VERSION`
  - Bumped from `0.1.126` to `0.1.127`.

### Verification

- `npx pnpm --dir frontend run typecheck`: passed
- `npx pnpm --dir frontend run lint:check`: passed
- `go test ./...`: passed
- `git diff --check`: passed
- Local preview:
  - Vite frontend: `http://localhost:3000/`
  - Proxy target: `https://staging.fuxiapi.top`
  - `http://localhost:3000/api/status`: 200
  - `https://staging.fuxiapi.top/api/status`: 200

### Product Behavior Confirmation

- Accounts showing `429` / `7d 100%` are not directly disabled by default.
- Backend 429 handling writes `rate_limited_at` and `rate_limit_reset_at`.
- Scheduler queries exclude those accounts while `rate_limit_reset_at` is in the future and allow automatic recovery after reset.
- Direct error/disabled handling remains reserved for authentication/permission failures, custom error-code policies, and other explicit stop-scheduling conditions.

### Notes

- This archive was updated after the production deploy for `0.1.127`.
- Release-prep commit pushed to `origin/main`: `41d81ff1 Prepare 0.1.127 account table release`.
- Archive commit pushed to `origin/main`: `84e75125 Archive 0.1.127 release prep`.

## 2026-05-18 - v0.1.127 Production Release

### Scope

- Complete the user-defined `更新发布版` workflow for `v0.1.127`.
- Confirm GitHub Release and CI status, deploy production, verify public runtime, and archive the corrected release semantics.
- Preserve old `F:\newAPI`, `/data/new-api/**`, `new-api`, and `new-api-staging` rollback resources.

### Release and CI

- Version: `0.1.127`
- Tag: `v0.1.127`
- Tag commit: `84e75125e76447023002d9da63c23987b322810f`
- GitHub Release: `https://github.com/jiutubaba/fx-api/releases/tag/v0.1.127`
- Release workflow `26035687293`: success
- Tag CI workflow `26035687297`: success
- Tag Security Scan workflow `26035687263`: success
- Main archive CI workflow `26036303890`: success
- Current main before deploy: `31b1fbdc Archive v0.1.127 release publication`

### Production Deploy

- Deploy time: 2026-05-18 21:33 Asia/Shanghai
- Runtime root: `/data/fuxi-api/prod`
- Command path: Docker Compose pull and recreate of `fuxi-api` only.
- Production image: `ghcr.io/jiutubaba/fx-api:latest`
- Initial image ID after deploy: `sha256:c59db6ae5717934229f0a7b9655202b564b308e94a1f0e4102516c16b11492e7`
- Final image ID after archive-commit GHCR sync: `sha256:16844dca16706e43490e1bb230bfcdc94d2bedba42136f3927a38f6ec62cd764`
- `fuxi-api-prod`: healthy, 0 restarts, `127.0.0.1:3300->8080/tcp`
- `fuxi-api-prod-redis`: healthy

### Verification

- Local `http://127.0.0.1:3300/api/status`: 200, `{"status":"ok"}`
- Public `https://fuxiapi.top/api/status`: 200, `{"status":"ok"}`
- Public `https://fuxiapi.top/`: 200
- Redis persistence: `rdb_last_bgsave_status=ok`, `aof_last_write_status=ok`
- Redis write probe: OK
- Authenticated `/v1/models`: 200 with 4 models using `key_id=10`
- Model sample: `gpt-5.3-codex`, `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.5`
- Real `/v1/chat/completions`: 200 using `gpt-5.4`; response sample `pong`
- Production account-pool state: `api_keys active=8`; `accounts active=29`; distribution `openai/apikey active=1`, `openai/oauth active=28`, `openai/oauth disabled=72`
- Archive commit `c239f0a5` and final sync archive commit `b572f9bc` CI, GHCR Image, and Security Scan workflows all succeeded; production was resynced to the final resulting `latest` image.

### Semantics Update

- User clarified that `更新发布版` and `更新发布版并归档` mean:
  - update the repository and push Git
  - update GitHub Release/tag
  - deploy production
  - really update `https://fuxiapi.top/`
- Project docs were updated to reflect this as the active rule.
- Production Caddy cutover/rollback target changes and deletion of protected legacy resources remain separately confirmation-gated.

### Notes

- No old `new-api` rollback resources were deleted or overwritten.
- The authenticated production checks used an active server-side API key without printing the secret.

## 2026-05-18 - Fuxi API sub2api Cutover

### Scope

- Replace the old new-api production framework with the new sub2api-based Fuxi API runtime.
- Preserve old `F:\newAPI`, `/data/new-api/**`, `new-api`, and `new-api-staging` as backup and rollback sources.
- Keep public domains unchanged:
  - Production: `https://fuxiapi.top`
  - Staging: `https://staging.fuxiapi.top`

### Repository

- Active repo: `https://github.com/jiutubaba/fx-api.git`
- Latest cutover commit: `a8dbd12b Configure fuxi proxy runtime defaults`
- CI, Security Scan, and GHCR build passed for the cutover commits.

### Migration Fixes

- `00f01be0 Migrate legacy account pool accounts`
- `0d007d76 Migrate legacy auto group bindings`
- `feb84b83 Fix fuxi redis volume ownership`
- `a8dbd12b Configure fuxi proxy runtime defaults`

### Production Data Migration

- Target DB: `fuxi_api_prod`
- Source DB: `new_api_prod`
- Fresh migration report: `/data/fuxi-api/prod/reports/prod-20260518-135648.md`
- Reported target counts:
  - `users`: 8
  - `api_keys`: 16
  - `accounts`: 101
  - `groups`: 5
  - `account_groups`: 231
  - `usage_logs`: 6852
- Known warnings:
  - 3807 consumption logs were archived only because old token/channel rows did not map to target rows.
  - Legacy custom OAuth bindings were archived only.

### Production Cutover

- Cutover time: 2026-05-18 14:25 Asia/Shanghai
- Caddy backup: `/etc/caddy/Caddyfile.bak.20260518-062510`
- New production target: `127.0.0.1:3300`
- Old production target retained: `127.0.0.1:3000`
- Production image after final pull: `sha256:59879147153e0714e6eaf5df61f4c51168d6a9d65b24d3ab8388a479d66b714e`
- Old image retained: `sha256:1d2ceddceec42982e998c71d040944895c013b2a699085fd6d8fcce5cc1f6305`

### Post-Cutover Verification

- `https://fuxiapi.top/api/status`: 200
- `https://fuxiapi.top/health`: 200
- `https://fuxiapi.top/`: 200
- Authenticated `https://fuxiapi.top/v1/models`: 200
- `free` group `gpt-5.4-mini` chat completion: 200
- `auto` group `gpt-5.4-mini` chat completion: 200
- `plus` group chat completion: 403 `INSUFFICIENT_BALANCE`, expected due to zero balance.
- Redis `rdb_last_bgsave_status=ok`, `aof_last_write_status=ok`, write test passed.
- No cutover-time panic, fatal, Redis MISCONF, or 5xx errors found in sampled logs.

### Rollback Command

```bash
CONFIRM_SWITCH=fuxiapi.top NEW_TARGET=127.0.0.1:3000 /data/fuxi-api/deploy/switch-prod-caddy.sh
```

## 2026-05-18 - Account and Channel Visibility Follow-up

### Scope

- Improve the admin account management page after production cutover.
- Investigate why user-facing available channels and channel status were blank in production.
- Keep old new-api resources preserved while deploying the fix to the new Fuxi API runtime.

### Findings

- Production feature flags were enabled:
  - `available_channels_enabled = true`
  - `channel_monitor_enabled = true`
- Production data shape after migration:
  - `channels`: 0 rows
  - `accounts`: 101 rows, including 29 active accounts
  - `groups`: 5 rows, all `openai` and active
  - `channel_monitors`: 0 rows
- Available channels were blank because the new sub2api UI/API read the formal `channels` table, while migrated usable resources live in `accounts` and `account_groups`.
- Channel status was blank because no monitor tasks/history had been configured or migrated. This was not a disabled setting.

### Code Changes

- Commit: `409cb4ee Improve account and channel visibility`
- `frontend/src/views/admin/AccountsView.vue`
  - Added an old-repo-style statistics strip for total accounts, enabled accounts on the current page, schedulable accounts on the current page, and abnormal/disabled accounts on the current page.
- `backend/internal/handler/available_channel_handler.go`
  - Added an account-pool fallback for `/api/v1/channels/available` when `channels` is empty and the user has accessible groups.
  - The fallback does not fabricate supported models when the production group has no default model or route configuration.
- `backend/internal/handler/available_channel_handler_test.go`
  - Added unit coverage for the fallback behavior.
- `frontend/src/views/user/ChannelStatusView.vue`
  - Added read-only account-pool status cards when real monitor data is empty.
  - Synthetic fallback cards do not open the monitor detail dialog.
- `frontend/src/api/channelMonitor.ts`
  - Allowed empty monitor status/availability values and marked synthetic rows.
- `frontend/src/components/user/monitor/MonitorHero.vue`
  - Added an `unmonitored` overall state so missing monitor configuration is not shown as degraded.
- `frontend/src/i18n/locales/zh.ts` and `frontend/src/i18n/locales/en.ts`
  - Added translations for account pool stats and unmonitored status.

### Verification

- Local:
  - `go test -tags unit ./internal/handler ./internal/service`: passed
  - `npx pnpm --dir frontend run typecheck`: passed
  - `npx pnpm --dir frontend run lint:check`: passed
  - `git diff --check`: passed
- CI/GHCR:
  - Security Scan: passed
  - GHCR Image: passed
  - Frontend CI, golangci-lint, and tests: passed
- Production deploy:
  - Pulled and restarted `ghcr.io/jiutubaba/fx-api:latest` in `/data/fuxi-api/prod`.
  - `fuxi-api-prod`: healthy
  - `https://fuxiapi.top/api/status`: 200 with GET
  - `https://fuxiapi.top/health`: 200
  - `https://fuxiapi.top/`: 200
  - Production compose shows `fuxi-api-prod` on `127.0.0.1:3300->8080/tcp`.
- Preservation check:
  - `/data/new-api` remains present.
  - `new-api` remains running on `127.0.0.1:3000->3000`.
  - `new-api-staging` remains running on `127.0.0.1:3100->3000`.

### Notes

- Use GET, not HEAD, when verifying `/api/status`; Gin only registers GET for that route.
- The channel status page now distinguishes "unmonitored" from degraded. Real latency/availability still requires configuring `channel_monitors`.
