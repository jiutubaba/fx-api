# Backlog

- Consider a follow-up cleanup pass for stale legacy references in comments or docs, without touching preserved backup projects.
- Add a fresh post-cutover monitoring note after 24 hours of production traffic.
- Decide whether `plus` group should receive balance or be disabled if the zero-balance 403 is not desired.

